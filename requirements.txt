PyGObject>=3.40.0
requests>=2.28.0
cryptography>=40.0.0
httpx>=0.27.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sse-starlette>=2.0.0
psutil>=5.9.0
textual>=1.0.0
pyperclip>=1.8.0
# cryptography is the only hard dep for core storage + crypto
# PyGObject requires system packages: python3-gi, gir1.2-gtk-3.0
# httpx, fastapi, uvicorn, sse-starlette are for the sidecar backend
# psutil is for system monitoring (optional, graceful fallback)
